{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "engines": {"node": "16.x"},
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd client && npm run build",
    "install-client": "cd client && npm install",
    "start": "npm run dev",
    "client": "cd client && npm start",
    "dev": "concurrently \"cd server && node server.js\" \"npm run client\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.4.0",
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "mongoose": "^6.6.0"
  }
}
